.DS_Store
.playwright-cli/
.workbuddy/
/preview.png
/preview-mobile.png
# 本地排查用的临时截图（不属于站点资源，README 也未引用）。
# 曾经被 `git add -A` 误提交过一次，共 1.6MB —— 显式 ignore 从根上避免复发。
/i18n-en.png
/i18n-zh.png
/readme-check.png
# 网站体验审计的生成物（体积大、属临时产物）
/output/
*.zip
node_modules/

# 敏感配置（含 AppSecret 等凭据，严禁提交）
.env
.env.*
!.env.example

# 部署与构建产物（EdgeOne 部署源 / 详情页 / 分类页，均由 build.mjs 生成）
.edgeone/
dist/
p/
c/
