# indiemaker.cn 改进台账

> 每日 Umami 日报的累积台账：日期 / 发现 / 建议 / 状态。
> 由 `automation-1789970377393`（每日 10:30）追加维护，日报正文在 `docs/umami/YYYY-MM-DD.md`。
> 本台账与 `docs/` 其余文件同属分析资料：**已入库，但不触发部署**
> （`docs/**` 在 `.github/workflows/update-and-deploy.yml` 的 `paths-ignore` 里）。
> 注意：是「不触发部署」，不是「不入库」—— 实测 `git ls-files docs/` 有 45 个文件在版本控制内，
> 改动仍需正常 commit。

---

## 2026-09-21

**窗口**：09-20 14:24 → 09-21 14:24（24h）
**访客 78（+13%）/ 访问 270（+141%）/ 浏览 410（+89%）/ 跳出 75.2%（↓恶化）/ 停留 5m3s（+59%）**

### 发现

1. **访问/访客 = 3.46**（正常 1–2.5）→ 混入自动化流量。
   噪音来源已定位：EdgeOne 预览路径，**会话口径 28 次（占访问 10.4%）**。
   根因：追踪守卫 9/20 21:30 才上线，之前的部署产物里没有守卫，被访问时照样上报。
   **守卫只管它之后生成的部署** → 历史残留会自然归零，不用处理。
2. **浏览/访问 = 1.52**（正常 2–4）→ 单页即走比例高，与噪音同源（预览访客全是单页），
   但也叠加了「首页动线」问题，等干净窗口再判。
3. **LLM 渠道出现（0 → 1）**，来源 `chatgpt.com`。全量口径累计 **3 人**，全是 chatgpt.com
   → GEO 的持续回音（llms.txt / 结构化数据 / 边界声明是 9/20 才补齐的）。
4. **Bing 系是最大自然搜索来源**：全量 cn.bing.com 10 + bing.com 6 = **16 人，占自然搜索 70%**。
   新站 Bing 收录快于 Google，符合规律；IndexNow 9/20 已接线，往后会更快。
5. **首页仍占绝对多数**：全量 Top10 中首页 **165 人 = 78%**；3,060 条 URL 只有 5 个真实页面被访问。
6. **英文版被真实使用**：`/en/` 3 人（非预览路径）；海外全量 46%（US 16% / HK 20%）。

### 建议（按投入产出比）

| # | 建议 | 状态 |
|---|---|---|
| 1 | **首页 → 内页动线改造**：hero 下方加「频道入口条」（榜单/场景/数据报告），卡片列表前插「本周新收录」横向卡 | ✅ **已完成并上线**（2026-09-21 15:4x） |
| 2 | **CI 日志 token 脱敏**：`edgeone makers deploy --json` 输出用 sed 掩掉 `eo_token` 与预览域名 | ✅ **已完成并验证**（线上运行日志只剩 `***REDACTED***`） |
| 3 | **读第一个完全干净的窗口**：守卫 9/20 21:30 上线 → 9/21 10:30 → 9/22 10:30 才是第一个干净窗口 | 等 9/22 10:30 |

### 第 1 条落地细节（供后续对照）

诊断被收窄到一句话：**首页 `<main>` 里的站内内页链接数是 0**（只有 header 2 条 + footer 2 条）。
所以「78% 堵在首页」不是用户不想点，是页面里根本没有别的路。

- 首页频道入口区：11 个入口，计数由构建期实时算
- 全站页脚分类导航：13 个分类 × 3060 个页面
- 补上「未分类」（588 个作品，占全站 20%，此前连首页热门胶囊都不含它）
- 移动端用 `display:contents` 把三层结构拍平成单行横滑，导流区高度恒定 82px，
  首卡 658 → 752，仍在 844 视口内（保住了此前专门做过的首屏优化）
- 桌面导流区 237px，首卡 757 → 1019（超出 900 折叠线）——
  **这是有意的取舍**：桌面用「一屏换 11 个真实入口」。若后续数据显示桌面跳出率恶化，
  第一优先是压这一块的高度，而不是撤掉入口。

### 下一步要看的指标（9/22 起）

1. 首页占比是否从 78% 下降（这是本次改动唯一的目标指标）
2. 榜单页 / 场景页 / 分类页是否开始出现在 Pages 排行里
3. `/c/uncategorized.html` 是否拿到访问（它是 588 个作品的唯一入口，改前几乎不可能被触达）
4. 自然搜索与 LLM 渠道是否继续增长（内容侧无变化，属对照）


### 备注

- 本条目由人工回填（首份日报为手工执行，未走自动化）。
- 渠道名在内部接口中是 **camelCase**（`organicSearch` / `llm`），按空格写法匹配会静默漏掉。
- 噪音占比**不要用 `metrics(type=path)` 的 y 求和**（同一访客看 N 页计 N 次，求和只是上限）；
  会话口径应看 `type=entry` 里有多少条是噪音路径。


---

## 2026-09-22

**窗口**：09-21 10:59 → 09-22 10:59（24h）　**首个「全窗口受守卫覆盖」的干净窗口**
**访客 56（-33%）/ 访问 63（-77%）/ 浏览 122（-70%）/ 跳出 82.5%（↓恶化）/ 停留 58s（-81%）**
派生比值：浏览/访问 1.94｜**访问/访客 1.13（正常 1–2.5 ✅）**

### 发现

1. **自动化噪音已基本清除 —— 这是今天最重要的一条。**
   访问/访客从 3.46 归位到 **1.13**。访问量 -77% 不是流量掉了，是**昨天的 275 次访问里绝大部分是自动化**。
   佐证：昨日设备结构 iOS 53/84（63%）→ 今日 iOS 18/56（32%）；昨 5m3s 停留被自动化拉高，今日 58s 才是真实水平。
   → **结论：剔除噪音后的「真基线」比昨天看起来的差，但终于可读了。**
   预览噪音绝对数 28 → **18 会话**（在降）；占比升到 28.6% 只是因为分母（总访问）塌缩，**不是守卫失效**。
   已验证 `index.html:59-63` 守卫逻辑正确（hostname 白名单 + `/indie-maker-directory-` 前缀拦截）。
   ⚠️ 提醒：以后看噪音要看**绝对会话数**，别看占比 —— 分母塌缩会制造假恶化。

2. **🎯 首页占比 78% → 33.9%，动线改造的目标指标首次改善。**
   更关键的是**榜单页/分类页首次进入 Pages 排行**：
   `/c/ai-tools.html` 3 人（且进入离开页 2 人，说明真的被点进去了）、`/best-ai-tools.html` 2、`/best-indie-games.html` 1、`/best-dev-tools.html` 1（进入页）。
   这正是 9/21 改动预期要发生的事。**但样本仅 56 人 + 噪音 28.6%，只能算初步正信号，需再观察 2–3 天。**

3. **自然搜索 1 → 4，且 100% 来自 Bing 系**（bing.com 2 + cn.bing.com 2），Google 依旧 **0**。
   继续印证「Bing 收录快于 Google」；IndexNow（9/20 接线）疑似开始见效。

4. **🔍 跳出 82.5% / 停留 58s 很可能是「指标口径缺陷」，不是体验缺陷。**
   实测详情页 CTA：`<a class="btn-primary" ... target="_blank" rel="noreferrer">访问官网 ↗</a>`。
   目录站的终点动作就是「把用户送去工具官网」——**用户满意离开同样被计为跳出**。
   而全站 `grep umami.track|data-umami-event` **零命中：一个自定义事件都没埋**，所以无法区分
   「达成离开」与「失望跳出」。**这是当前最大的观测盲区。**

5. **`/$u` 拿到 3 人（404）—— 判定为外部探测，非本站故障，不处理。**
   排查：全仓 grep 无 `$u` 字面量、sitemap 无、线上首页 HTML 无，curl 返回 404。
   → 典型「模板变量未展开」的外部请求特征。仅登记观察，无需改代码。

6. **更正一条此前的错误假设：内链并不缺。**
   实测 `/p/jev-social.html` 已有 同分类推荐 8 条 + 分类链接 4 条 + 页脚；`/c/ai-tools.html` 有 120 条内链。
   结构化数据也已完备（分类页 CollectionPage+ItemList；详情页 SoftwareApplication+BreadcrumbList+Offer）。
   → **「加相关推荐/加结构化数据」这类建议是无效的，已排除，避免浪费工时。**

7. **意外发现外链资产**：`yeyulingfeng.com/a/997983.html`（8/30）是一篇**第三方独立推荐长文**，
   含首页全貌截图与三步使用法。属真实第三方报道，此前未登记。当前全站 Referral 仅 2 人（w2solo 1、moz 1），
   说明「有报道 ≠ 有导流」，这类文章的转化路径值得单独看。

### 建议（按投入产出比）

| # | 建议 | 状态 |
|---|---|---|
| 1 | **给「访问官网」外链 CTA 埋自定义事件**，把 82.5% 跳出拆成「达成离开 / 真跳出」 | 待办 |
| 2 | **补 Google 这个 0 渠道**：确认 GSC 收录状态 + sitemap 已提交（需人工操作） | 待办 |
| 3 | **分类页加 FAQ 块 + FAQPage 结构化数据**，吃长尾口语查询 | 待办 |

### 第 1 条落地要点（最高优先级）

- 位置：`index.html` 的守卫 IIFE 内（第 57–70 行那段）追加全局委托监听，
  判 `a.target === "_blank"` 或 `rel` 含 `noreferrer` → `umami.track("visit-official", {slug})`。
- ⚠️ `postProcess()` 是唯一全局注入入口，**改一处即可覆盖全站**；但 `en/` 是 `en-pages.mjs` 单独生成的，需同步。
- 判读标准：若外链点击率 >40%，说明 82.5% 跳出以「达成离开」为主，
  优化重心应转向「停留质量」；若 <15%，才是真的要治内页动线。

### 下一步要看的指标（9/23 起）

1. 访问/访客是否稳定在 1–2.5（确认噪音清零后不再反弹）
2. 预览噪音**绝对会话数**是否继续从 18 下降
3. 首页占比能否稳在 40% 以下 + 分类页是否持续有量
4. 自然搜索（Bing）是否延续 1→4 的上行
5. **先补观测口径，再谈优化** —— 在埋点上线前，跳出率/停留时长不要作为决策依据

### 备注

- 本次为自动化首次执行（`automation-1789970377393`），此前 9/21 条目为人工回填。
- 全程未改动任何站点代码、未 commit / push（本次无故障需要修）。
